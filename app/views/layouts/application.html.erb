<!DOCTYPE html>
<html>
<head>
  <title>Depot</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body class='<%= controller.controller_name %>'>
    
      <div class="banner">
        <div class="container">
            
            <h1 class="text-right"><%= image_tag ('logo.png') %> <%= @page_title || 'Личный магазин Его высочества Никитки' %></h1>
            <p class="text-right"><%= @page_subtitle || "Все доходы идут на улучшение жизни Никиток мира." %></p>
        </div>
      </div>
      
    <div class="container-fluid">
        <div class ="row">
            <div class="col-md-7 col-md-offset-2">
                <%= yield %>
            </div>
            
            <div class="col-md-3">
                <h2>Разделы</h2>
                <nav class="navbar navbar-default" role="navigation">
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse navbar-ex1-collapse">
                  <ul class="nav nav-pills nav-stacked">
                    <li <% if(controller.controller_name == 'store')  %> class="active"<% end %> role="presentation"><%= link_to 'Каталог', store_path %> </li>
                    <li <% if(controller.controller_name == 'products')  %> class="active"<% end %> role="presentation"><%= link_to 'Управление товарами', products_path %></li>
                    <li <% if(controller.controller_name == 'carts')  %> class="active"<% end %> role="presentation"><%= link_to 'Корзина', cart_path(session[:cart_id]) %></li>
                    
                  </ul>
                </div><!-- /.navbar-collapse -->
              </nav>
                <h4 class='text-right'>Поздравляем вас, сейчас:<br/><small><%= @time %></small></h4>
            </div>
        </div>
    </div>

</body>
</html>
